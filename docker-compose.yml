services:
  holocron:
    build: .
    container_name: holocron
    restart: unless-stopped
    volumes:
      - ./mirror-data:/app/mirror-data
    env_file:
      - .env
    # Optional: Override command
    # command: ["--watch", "--interval", "30", "--backup-only"]
